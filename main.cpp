#include <iostream>
#include "common.h"
#include <fstream>
#include <chrono>

int main() {

    std::string zodziai; // Ivedimo string
    std::string hashOutput;

    fileChoice();

    //konstitucijosTestas(zodziai);

    return 0;
}
